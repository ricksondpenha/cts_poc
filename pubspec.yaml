name: cts_poc
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  #Architecture and State related dependencies
  go_router: ^3.0.1
  flutter_riverpod: ^2.0.0-dev.0
  freezed_annotation:
  json_annotation: ^4.4.0

  intl: ^0.17.0
  http: ^0.13.4
  graphql: ^5.0.0
  auto_size_text: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  build_runner:
  freezed:
  json_serializable: ^6.1.3

flutter:
  uses-material-design: true
  assets:
    - assets/fonts/
    - assets/i18n/
    - assets/img/
